[project]
name = "mock-firmware"
version = "0.1.0"
description = "Mock ESP32 firmware HTTP server for BoatWatch app development"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
mock-firmware = "mock_firmware:main"
